{
  "name": "investing-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run check-markers && vite --host --port 5173",
    "build": "npm run check-markers && vite build",
    "preview": "vite preview --host --port 3000",
    "check-markers": "node check-markers.js"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "vite": "5.4.2",
    "terser": "^5.24.0"
  }
}
